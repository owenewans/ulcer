#!/usr/sbin/nft -f

# Ulcer owns this table only. Reconciliation must use atomic netlink batches and
# must never flush the host ruleset or mutate operator-owned tables.
table inet ulcer {
	comment "ulcer managed; do not edit"
}
